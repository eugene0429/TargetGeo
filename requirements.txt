torch>=2.0
opencv-python>=4.8
numpy>=1.24
Pillow>=10.0
huggingface_hub>=0.20
pymap3d>=3.0
ultralytics>=8.0
sam3 @ git+https://github.com/facebookresearch/sam3

# sam3 runtime deps it under-declares in its own metadata. Its public
# build_sam3_image_model import path transitively pulls in the training stack,
# so these are required even for inference-only use.
einops>=0.7
pycocotools>=2.0
hydra-core>=1.3
omegaconf>=2.3
submitit>=1.5
torchmetrics>=1.0

pytest>=7.0          # test suite
